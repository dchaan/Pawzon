<!DOCTYPE html>
<html>
  <head>
    <title>Pawzon</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta property="og:title" content="Pawzon" />
    <meta property='og:image' content="<%= image_url('/Pawzon/app/assets/images/project_pic.png') %>"/>
    <meta property="og:description"
      content="Pawzon is an Amazon clone with a focus on dog products. 
              User authorization allows anyone to view the home page and 
              product pages, but only logged in users can add items to the cart and checkout. 
              Product quantities can be edited before being adding to the cart or while the product is
              in the cart. Products can also be deleted from the cart. Checking out displays a checkout
              confirmation message since real orders cannot be made."
    />
    <meta property="og:url"
      content="http://projectname.herokuapp.com/"
    />

    <% if current_user %>
      <script>
        window.currentUser = {
          'id': '<%=current_user.id%>',
          'email': '<%=current_user.email%>',
          'first_name': '<%=current_user.first_name%>',
          'last_name': '<%=current_user.last_name%>'
        }
      </script>
    <%end%>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
